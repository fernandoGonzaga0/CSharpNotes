/*

Serialização -> é o processo de conversão do estado de um objeto em um fluxo de bytes (stream) de forma que ossa ser armazenado na memória, em arquivo ou em um banco de dados.

    Tipos: 
            binária (preserva a fidelidade do tipo e estado de um objeto -OBSOLETA-);
            XML ou SOAP (serializa somente propriedades públicas, não preserva a fidelidade de tipo);
            JSON (serializa somente propriedades públicas, não preserva fidelidade de tipo);

Desserialização -> é o processo reverso da serialização.

*/
